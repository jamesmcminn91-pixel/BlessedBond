# Prisma Notes

Blessed Bond is configured for Postgres in production.

If you are using Vercel, prefer a hosted Postgres database over SQLite.

Useful commands:
- npm run db:generate
- npm run db:push
- npm run db:seed